//---------------------------------------------------------------------------

#ifndef BDDH
#define BDDH
//---------------------------------------------------------------------------

#include <string>

using namespace std;
class BDD {

private:

public:

	BDD();
	~BDD();
	virtual void connect(string host,string user,string password,string dbname)=0;
	virtual void insert(string requete)=0;
	virtual void select(string requete)=0;
//	virtual void saveBDD(string requete)=0;
//	virtual void restorBDD(string requete)=0;
//	virtual void compareBDD(string requete)=0;

};

#endif
