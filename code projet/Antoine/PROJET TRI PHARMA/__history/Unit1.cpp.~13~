//---------------------------------------------------------------------------

#include <vcl.h>
#pragma hdrstop

#include "Unit1.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)
#pragma resource "*.dfm"
TForm1 *Form1;
//---------------------------------------------------------------------------
__fastcall TForm1::TForm1(TComponent* Owner)
	: TForm(Owner)
{
	sql.connect("127.0.0.1","root","","pharmacie");

	string requete="INSERT INTO pharmacie.test (id) VALUES (25);";
//	requete+=",medoc,";// a mettre une variable contenant le médicament
//	requete+="15,";    // a mettre une variable contenant la hauteur
//	requete+="15,";    // a mettre une variable contenant la largueur
//	requete+="15,";    // a mettre une variable contenant la longueur
//	requete+="1,";     // a mettre une variable contenant la quantite restante
//	requete+="code,";  // a mettre une variable contenant le code barre
//	requete+="17.5,";  // a mettre une variable contenant le prix
//	requete+="60);";    // a mettre une variable contenant le nbr unite vendu


	sql.insert(requete);
}
//---------------------------------------------------------------------------
